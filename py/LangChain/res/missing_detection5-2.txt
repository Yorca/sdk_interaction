Based on the provided app runtime context and the summaries of the privacy APIs, here are the APIs that should be invoked:

1. **SetMetaData (Unity Ads)**:
   - **Conditions**:
     - The user is under the age of 13 (indicated by `set Coppa to true`).
     - The app is child-directed (indicated by `set Child Directed Treatment to true`).
   - **Effects**:
     - Flags the user as child-directed for compliance with child-related regulations.
     - Sets privacy-related metadata for the user regarding personal information handling.
   - **Parameter Configurations**:
     - Set `is_child_directed` to `true`.

2. **setMetaData (IronSource)**:
   - **Conditions**:
     - The app is child-directed and the user is flagged as a child under applicable regulations (as indicated by `set Child Directed Treatment to true` and `set Coppa to true`).
     - The user opts out of the sale or sharing of their personal information (as indicated by `set Do Not Sell flag to true`).
   - **Effects**:
     - Flags the user as child-directed.
     - Handles the 'do not sell or share' setting for U.S. state privacy laws.
   - **Parameter Configurations**:
     - Set `is_child_directed` to `true`.
     - Set `do_not_sell` to `true`.

3. **setConsent (IronSource)**:
   - **Conditions**:
     - The user has consented to data processing under GDPR (as indicated by `set Has User Consent to true`).
   - **Effects**:
     - Updates the user's consent status for data processing.
   - **Parameter Configurations**:
     - Set the consent flag to `true`.

4. **SetMetaData (Unity Ads)** (for GDPR compliance):
   - **Conditions**:
     - User opts in to targeted advertising under GDPR compliance (as indicated by `set Has User Consent to true`).
   - **Effects**:
     - User consents to receiving targeted advertisements.
   - **Parameter Configurations**:
     - Set `gdpr.consent` to `true`.

Based on the conditions met in the runtime context, it is necessary to invoke the aforementioned APIs to ensure compliance with privacy regulations, particularly those related to children and GDPR.