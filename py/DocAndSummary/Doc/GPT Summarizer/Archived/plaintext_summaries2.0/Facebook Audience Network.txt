{
    "SDK": "Facebook Audience Network",
    "privacy_APIs": [
        {
            "API_name": "setDataProcessingOptions",
            "conditions": [
                "must enable Limited Data Use (LDU) for users in California, Colorado, or Connecticut",
                "must specify correct location parameters for data processing"
            ],
            "effects": [
                "enables control over data usage in compliance with US state privacy regulations",
                "limits data usage as specified in State-Specific Terms"
            ],
            "parameter_configurations": [
                {
                    "parameter_values": [
                        [], // To not enable LDU
                        ["LDU"], 0, 0, // To enable LDU and have Meta perform geolocation
                        ["LDU"], 1, 1000 // To enable LDU and manually specify location for California
                    ],
                    "conditions": [
                        "if no LDU option is provided, no changes to data processing occur",
                        "geolocation will be performed if enabled",
                        "manual location must be specified to process data accordingly"
                    ],
                    "effects": [
                        "data will not be processed under Limited Data Use",
                        "data will be processed with location data when geolocation is enabled",
                        "specific state regulations will be applied when manually specifying location"
                    ]
                }
            ]
        },
        {
            "API_name": "setAutoLogAppEventsEnabled",
            "conditions": [
                "must obtain user consent to log events under GDPR regulations"
            ],
            "effects": [
                "enables or disables automatic logging of app events"
            ],
            "parameter_configurations": [
                {
                    "parameter_values": [
                        false, // to disable automatic logging before consent
                        true // to enable logging after consent is given
                    ],
                    "conditions": [
                        "if user has not provided consent, automatic logging must be disabled",
                        "automatic logging can only be enabled after user consent"
                    ],
                    "effects": [
                        "no events will be logged if automatic logging is disabled",
                        "events will be logged automatically if consent is granted and logging is enabled"
                    ]
                }
            ]
        },
        {
            "API_name": "setAdvertiserTrackingEnabled",
            "conditions": [
                "user must provide permission for app tracking"
            ],
            "effects": [
                "enables or disables advertiser tracking"
            ],
            "parameter_configurations": [
                {
                    "parameter_values": [
                        true, // to allow advertiser tracking
                        false // to disallow advertiser tracking
                    ],
                    "conditions": [
                        "if permission is granted, advertiser tracking can be enabled",
                        "if permission is denied, advertiser tracking must be disabled"
                    ],
                    "effects": [
                        "app events will be sent with tracking enabled when true",
                        "app events will not be tracked when false"
                    ]
                }
            ]
        }
    ]
}