{
    "SDK": "Smart AdServer",
    "privacy_APIs": [
        {
            "API_name": "retrieveUserConsent",
            "class_name": "",
            "conditions": [
                "User must provide consent through a compliant Consent Management Provider (CMP) as per GPP or TCFv2 regulations."
            ],
            "effects": [
                "User identifiers such as UID, IFA, IFV, and DNTID are sent in ad calls only if consent is granted."
            ],
            "parameter_configurations": []
        },
        {
            "API_name": "setConsentString",
            "class_name": "",
            "conditions": [
                "Consent string must be stored in `SharedPreferences` on Android or `NSUserDefaults` on iOS using key `IABTCF_TCString`."
            ],
            "effects": [
                "Sets the user's consent string which is used for GDPR compliance."
            ],
            "parameter_configurations": [
                {
                    "parameter_values": [
                        "yourCMPComputedConsentStringBase64format"
                    ],
                    "conditions": [
                        "This is specifically for CMPs that comply with IAB specifications."
                    ],
                    "effects": [
                        "Enables the SDK to retrieve and utilize the consent signals for ad requests."
                    ]
                }
            ]
        },
        {
            "API_name": "setCCPAString",
            "class_name": "",
            "conditions": [
                "CCPA consent string must be stored in `SharedPreferences` on Android or `NSUserDefaults` on iOS using key `IABUSPrivacy_String`."
            ],
            "effects": [
                "Sets the user's consent string for CCPA compliance."
            ],
            "parameter_configurations": [
                {
                    "parameter_values": [
                        "1---"
                    ],
                    "conditions": [
                        "This must be done to inform the SDK regarding the user's consent status under CCPA."
                    ],
                    "effects": [
                        "Allows for the forwarding of the consent string to third parties by the SDK."
                    ]
                }
            ]
        },
        {
            "API_name": "automaticLocationRetrieval",
            "class_name": "",
            "conditions": [
                "User must have given consent for location retrieval, particularly if under GPP compliance."
            ],
            "effects": [
                "Automatically retrieves the user's location to enhance advertising revenue, but does not send location if consent is not granted."
            ],
            "parameter_configurations": []
        }
    ]
}