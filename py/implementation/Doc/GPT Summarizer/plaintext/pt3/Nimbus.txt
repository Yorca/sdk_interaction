{
    "SDK": "Nimbus",
    "privacy_APIs": [
        {
            "API_name": "setGdprConsent",
            "class_name": "NimbusAdManager / RequestManager",
            "conditions": [
                "Consent string must be generated according to GDPR regulations",
                "Must be called after setting up a Consent Management Platform"
            ],
            "effects": [
                "Sets the GDPR consent string for the user",
                "Overrides existing GDPR values set in SharedPreferences"
            ],
            "parameter_configurations": [
                {
                    "parameter_values": [
                        "consentString"
                    ],
                    "conditions": [
                        "The consentString is properly formatted as per GDPR requirements"
                    ],
                    "effects": [
                        "User's consent status for data protection is updated according to the consent string provided"
                    ]
                }
            ]
        },
        {
            "API_name": "setUser",
            "class_name": "NimbusAdManager / RequestManager",
            "conditions": [
                "Must be called after setGdprConsent if you want the new user values to overwrite previous consents"
            ],
            "effects": [
                "Sets the user object which can include overridden consent values"
            ],
            "parameter_configurations": [
                {
                    "parameter_values": [
                        "user"
                    ],
                    "conditions": [
                        "User object is instantiated with the necessary consent information"
                    ],
                    "effects": [
                        "All subsequent requests will include the updated user consent settings"
                    ]
                }
            ]
        },
        {
            "API_name": "setUsPrivacyString",
            "class_name": "Nimbus",
            "conditions": [
                "The privacy string must conform to CCPA regulations and be set before initializing any Demand Providers"
            ],
            "effects": [
                "Sets the CCPA privacy string for the user"
            ],
            "parameter_configurations": [
                {
                    "parameter_values": [
                        "privacyString"
                    ],
                    "conditions": [
                        "The privacyString is within the required 4-character format"
                    ],
                    "effects": [
                        "User's consent status under CCPA is communicated to demand partners"
                    ]
                }
            ]
        },
        {
            "API_name": "setCOPPA",
            "class_name": "Nimbus",
            "conditions": [
                "Applicable for apps governed by COPPA regulations",
                "Must be called after SDK initialization"
            ],
            "effects": [
                "Configures the SDK to comply with COPPA requirements"
            ],
            "parameter_configurations": [
                {
                    "parameter_values": [
                        true
                    ],
                    "conditions": [],
                    "effects": [
                        "Enables COPPA compliance for demand partners"
                    ]
                }
            ]
        }
    ]
}